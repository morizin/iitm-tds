on:
  push:
    branches: [ main ]
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: 24f1002455@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: prime-cache-30145bd  # ✅ Correct step name
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.npm  # ✅ Better: actual npm cache path
          key: cache-30145bd  # ✅ Perfect

      - name: Cache hit/miss result
        run: echo "Cache hit: ${{ steps.cache-step.outputs.cache-hit }}"  # ✅ 
